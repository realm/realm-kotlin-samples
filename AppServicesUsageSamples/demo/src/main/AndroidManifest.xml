<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name=".AppServicesUsageSamplesApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppServicesUsageSamplesTheme"
        tools:targetApi="31">
        <activity
            android:name=".SampleSelectorActivity"
            android:exported="true"
            android:label="@string/title_activity_example_selector"
            android:theme="@style/Theme.AppServicesUsageSamplesTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".propertyencryption.PropertyEncryptionActivity" android:launchMode="singleTop"/>
        <activity android:name=".presence.PresenceDetectionActivity"/>
        <activity android:name=".errorhandling.ErrorHandlingActivity"/>
    </application>

</manifest>